{
  "name": "afiliados-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview --port $PORT --host"
  },
  "dependencies": {
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}

